<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (content !== null) { %>
      <ol>
        <% for (let i in content) { %>
          <% const obj = content[i]; %>
          <li><a href="<%= obj.link %>"><%= obj.title %></a></li>
        <% } %>
      </ol>
    <% } %>
  </body>
</html>
